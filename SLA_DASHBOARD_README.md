# SLA Dashboard - Documentation

## Обзор

Страница SLA Dashboard (`/app/sla`) предоставляет real-time метрики производительности склада на основе существующих данных. Никаких новых SQL таблиц не создается - все метрики вычисляются из:
- `units` - статусы и возраст заказов
- `audit_events` - история событий
- `outbound_shipments` - OUT отправки и возвраты
- `picking_tasks` - производительность задач

## Доступ

**Кнопка:** В верхнем меню справа (TopBar) появилась кнопка **"📊 SLA"**

**Права доступа:** Все авторизованные пользователи могут просматривать метрики своего склада

## Метрики

### 1. Ключевые показатели (KPI Cards)

#### Всего заказов
- **Значение:** Общее количество units в системе
- **Цвет:** Синий (#2563eb)

#### Залежалые заказы
- **Значение:** Количество заказов старше 24 часов (исключая shipped и out)
- **Цвет:** 
  - 🔴 Красный - если есть залежалые заказы
  - 🟢 Зелёный - если все обработаны быстро
- **Критерий:** Заказы со `created_at < now() - 24h`

#### Среднее время обработки
- **Значение:** Среднее время от создания unit до отгрузки (в часах)
- **Цвет:** Оранжевый (#f59e0b)
- **Расчёт:** На основе `audit_events` (unit.create → logistics.ship_out) за последние 7 дней

#### Процент возвратов
- **Значение:** % заказов, вернувшихся из OUT обратно на склад
- **Цвет:**
  - 🔴 Красный - если > 20%
  - 🟢 Зелёный - если ≤ 20%
- **Расчёт:** `(returned_shipments / total_shipments) * 100` за 7 дней

### 2. Диаграммы

#### Распределение заказов по статусам (Bar Chart)
- Показывает текущее количество units в каждом статусе
- Цветовая кодировка:
  - 🔵 receiving - синий
  - 🟣 bin - фиолетовый
  - 🟢 storage - зелёный
  - 🟠 picking - оранжевый
  - 🔴 shipping - красный
  - 🟣 out - фиолетовый
  - 🔵 transfer - голубой

#### Залежалые заказы (>24ч) по статусам (Bar Chart)
- Показывает распределение "проблемных" заказов
- Если нет залежалых заказов → показывает "Нет залежалых заказов 🎉"

#### Производительность (Donut Charts)
- **Задачи выполнены:** % completed picking tasks из общего количества
- **Успешно доставлено:** % заказов, которые НЕ вернулись из OUT

#### 🚨 Топ-10 самых долгих заказов (Table)
- Список заказов, находящихся на складе дольше всего
- Колонки:
  - Заказ (barcode)
  - Статус (с цветовым badge)
  - Возраст в часах
- Сортировка: от самого старого к новому
- Цвет возраста:
  - 🟠 > 24ч (оранжевый)
  - 🔴 > 48ч (красный)

### 3. Дополнительная статистика

#### Среднее время picking
- Среднее время выполнения picking задачи (created → completed)
- За последние 7 дней

#### OUT отправки
- Общее количество отправок за 7 дней
- Количество возвращённых

#### Процент завершенных задач
- Соотношение completed / total picking tasks
- За последние 7 дней

## API Endpoint

### `GET /api/sla/metrics`

**Доступ:** Все авторизованные пользователи

**Response:**
```json
{
  "ok": true,
  "metrics": {
    "total_units": 150,
    "units_over_24h": 5,
    "avg_processing_time_hours": 12.5,
    "units_by_status": {
      "receiving": 10,
      "storage": 50,
      "picking": 20,
      "shipping": 15,
      "out": 5
    },
    "old_units_by_status": {
      "storage": 3,
      "picking": 2
    },
    "out_total_shipments": 30,
    "out_returned_shipments": 5,
    "out_return_rate_percent": 16.7,
    "picking_avg_time_hours": 2.3,
    "picking_total_tasks": 45,
    "picking_completed_tasks": 42,
    "top_oldest_units": [
      {
        "barcode": "123456",
        "status": "storage",
        "age_hours": 72,
        "created_at": "2026-01-15T10:00:00Z"
      }
    ],
    "age_distribution": {
      "under_1h": 0,
      "1_6h": 0,
      "6_12h": 0,
      "12_24h": 0,
      "24_48h": 2,
      "over_48h": 3
    }
  }
}
```

**Security:** 
- Фильтрация по `warehouse_id` текущего пользователя
- Использует `supabaseAdmin` для избежания RLS рекурсии
- Проверка аутентификации и профиля

## Особенности UI

### Автообновление
- Метрики автоматически обновляются каждую минуту (60 секунд)
- Кнопка "🔄 Обновить" для ручного обновления

### Адаптивный дизайн
- Grid layout с `auto-fit` для адаптации под разные экраны
- Карточки автоматически перестраиваются

### Визуальная иерархия
- Большие цифры (32px, 28px) для ключевых метрик
- Цветовая кодировка для быстрого визуального анализа
- Тени и округления для современного вида

### 📝 Информативные пояснения
- **Под каждой метрикой** добавлено пояснение:
  - Источник данных (таблица в БД)
  - Формула расчета
  - Временной период
- **В KPI карточках:** иконка "?" при наведении раскрывает подробную информацию
- **В диаграммах:** текстовое описание под заголовком
- **Цель:** пользователь сразу видит, откуда берутся цифры и что они означают

### Компоненты

#### `MetricCard`
- Показывает одну метрику с заголовком, значением, подзаголовком
- Настраиваемый цвет
- **Новое:** Иконка "?" с tooltip-информацией при наведении
- **Новое:** Раскрывающееся пояснение о источнике данных

#### `BarChart`
- Горизонтальная bar chart с автоматическим масштабированием
- Плавная анимация при изменении данных
- **Новое:** Текстовое описание источника данных под заголовком

#### `DonutChart`
- Круговая диаграмма для процентных показателей
- SVG с анимированным прогрессом
- Показывает процент в центре
- **Новое:** Пояснение под заголовком блока

## Use Cases

### 1. Ежедневный мониторинг
**Кто:** Manager, Head, Admin
**Как:** Открыть `/app/sla` утром для проверки состояния склада

### 2. Выявление проблем
**Кто:** Ops, Manager
**Как:** 
- Посмотреть "Залежалые заказы"
- Проверить "Топ-10 самых долгих"
- Принять меры для ускорения обработки

### 3. Оценка производительности команды
**Кто:** Head, Admin
**Как:**
- Посмотреть "Среднее время picking"
- Оценить "Процент завершенных задач"
- Сравнить с плановыми показателями

### 4. Анализ качества доставки
**Кто:** Logistics, Manager
**Как:**
- Проверить "Процент возвратов"
- Если высокий → проанализировать причины в архиве

## Интеграция с существующим кодом

### TopBarExcel
Добавлена кнопка "📊 SLA" справа в первой строке:
- При наведении: меняет цвет на primary
- При клике: переход на `/app/sla`
- Не влияет на существующий функционал (barcode, зоны)

### Без изменений в БД
- ✅ Никаких новых таблиц
- ✅ Никаких новых индексов
- ✅ Только SELECT запросы
- ✅ Использует существующие данные

## Performance

### Оптимизация запросов
- Лимиты на количество строк (100 для old units, 500 для events)
- Временные фильтры (7 дней для событий)
- Использование индексов (created_at, warehouse_id)
- Агрегация на уровне приложения (не в SQL)

### Кэширование
- `cache: "no-store"` для всегда актуальных данных
- Автообновление каждую минуту (не слишком часто)

## Future Improvements

1. **Экспорт в Excel/PDF** - для отчетов менеджменту
2. **Исторические графики** - тренды по дням/неделям
3. **Алерты** - уведомления при превышении SLA
4. **Сравнение периодов** - сравнить текущую неделю с предыдущей
5. **Drill-down** - клик на метрику → детальный список заказов
6. **Фильтры по датам** - выбор временного диапазона
7. **Экспорт данных** - CSV export для анализа

## Troubleshooting

### Метрики не загружаются
**Проверить:**
```sql
-- Есть ли данные?
SELECT COUNT(*) FROM units;
SELECT COUNT(*) FROM audit_events;
SELECT COUNT(*) FROM outbound_shipments;
SELECT COUNT(*) FROM picking_tasks;
```

### Медленная загрузка
**Решение:** Добавить индексы (если еще не существуют):
```sql
CREATE INDEX IF NOT EXISTS idx_units_created_at ON units(created_at DESC);
CREATE INDEX IF NOT EXISTS idx_audit_events_created_at ON audit_events(created_at DESC);
```

### Неточные метрики
**Причина:** Данные за последние 7 дней могут быть неполными
**Решение:** Дождаться накопления данных или увеличить временное окно в API

---

**Готово к использованию! Все метрики real-time и обновляются автоматически.** 📊
