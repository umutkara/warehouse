# 📋 Автоматическая инвентаризация с заданиями

## 🎯 Обзор функциональности

Полностью автоматизированная система инвентаризации склада с заданиями на каждую ячейку, отслеживанием прогресса и автоматической генерацией отчётов.

## ✨ Основные возможности

### Для менеджеров:

1. **Запуск инвентаризации**
   - Нажатие одной кнопки "Начать инвентаризацию"
   - Автоматическое создание заданий на все активные ячейки
   - Блокировка всех перемещений на складе

2. **Мониторинг прогресса в реальном времени**
   - Страница `/app/inventory-progress` с визуальным прогрессом
   - Отображение процента выполнения (0-100%)
   - Список ячеек с статусами:
     - 🟡 Pending (ожидают) - жёлтые
     - 🟢 Scanned (отсканированы) - зелёные
   - Автообновление каждые 5 секунд
   - Информация о том, кто и когда отсканировал каждую ячейку

3. **Автоматическое завершение**
   - Когда все ячейки отсканированы → инвентаризация автоматически закрывается
   - Перемещения разблокируются автоматически
   - Возможность ручного завершения (если требуется)

4. **Генерация и хранение отчётов**
   - Кнопка "📥 Скачать отчёт" → CSV файл
   - Отчёт содержит:
     - Общую статистику (всего ячеек, отсканировано, расхождений)
     - Детали по каждой ячейке (ожидалось / найдено)
     - Расхождения: недостачи, излишки, неизвестные заказы
   - Автоматическое сохранение на сервер (Supabase Storage)
   - Аудит: кто и когда сгенерировал отчёт

### Для складчиков (ТСД):

1. **Простой рабочий процесс**
   - Выбор режима "Инвентаризация" в ТСД
   - Сканирование ячейки (код или штрихкод)
   - Сканирование всех заказов в ячейке (неограниченно)
   - Нажатие "Сохранить ячейку"

2. **Автоматическая синхронизация**
   - При сохранении ячейки:
     - Статус задания меняется с "pending" на "scanned"
     - База данных обновляется согласно факту
     - Система показывает расхождения (если есть)
     - Автоматически проверяется возможность закрытия инвентаризации

3. **Обратная связь**
   - Показывает расхождения:
     - ❌ Не найдено - заказы которые должны быть
     - ⚠️ Лишние - заказы которых не должно быть
     - ❓ Неизвестные - заказы которых нет в системе

## 🗂️ Структура базы данных

### Таблица `inventory_sessions`
```sql
- id (uuid, PK)
- warehouse_id (uuid, FK)
- status ('active' | 'closed')
- started_by (uuid, FK -> auth.users)
- started_at (timestamptz)
- closed_by (uuid, FK -> auth.users, nullable)
- closed_at (timestamptz, nullable)
```

### Таблица `inventory_cell_counts`
```sql
- id (uuid, PK)
- session_id (uuid, FK)
- cell_id (uuid, FK)
- scanned_by (uuid, FK -> auth.users)
- scanned_at (timestamptz)
- status ('pending' | 'scanned' | 'confirmed')
- meta (jsonb)
- UNIQUE(session_id, cell_id)
```

**Статусы заданий:**
- `pending` - задание создано, ячейка ещё не отсканирована
- `scanned` - ячейка отсканирована складчиком
- `confirmed` - (зарезервировано для будущего использования)

### Таблица `inventory_cell_units`
```sql
- id (uuid, PK)
- session_id (uuid, FK)
- cell_id (uuid, FK)
- unit_id (uuid, FK -> units, nullable)
- unit_barcode (text)
- created_at (timestamptz)
- UNIQUE(session_id, cell_id, unit_barcode)
```

## 📡 API Endpoints

### 1. POST `/api/inventory/start`
Запускает инвентаризацию
- **Доступ**: admin, head, manager
- **Действия**:
  - Создаёт новую сессию
  - Создаёт задания (`pending`) на все активные ячейки
  - Блокирует перемещения (`warehouses.inventory_active = true`)
- **Ответ**: `{ ok: true, sessionId, startedAt, tasksCreated }`

### 2. POST `/api/inventory/stop`
Завершает инвентаризацию вручную
- **Доступ**: admin, head, manager
- **Действия**:
  - Закрывает сессию
  - Разблокирует перемещения
  - Логирует количество незавершённых заданий
- **Ответ**: `{ ok: true, pendingTasks }`

### 3. GET `/api/inventory/tasks?sessionId=...`
Получает список заданий для отображения прогресса
- **Доступ**: authenticated users
- **Ответ**: 
```json
{
  "ok": true,
  "sessionId": "uuid",
  "tasks": [
    {
      "id": "uuid",
      "cellId": "uuid",
      "cellCode": "STOR-01",
      "cellType": "storage",
      "status": "pending" | "scanned",
      "scannedBy": "uuid",
      "scannedAt": "2025-01-19T12:00:00Z",
      "scannedByName": "Иван Иванов"
    }
  ]
}
```

### 4. POST `/api/inventory/cell-scan`
Сканирование ячейки с заказами
- **Доступ**: authenticated users
- **Body**: `{ cellCode: "STOR-01", unitBarcodes: ["123", "456"] }`
- **Действия**:
  - Обновляет статус задания: `pending` → `scanned`
  - Сохраняет список отсканированных заказов
  - Применяет изменения к реальным `units.cell_id`
  - Проверяет возможность автозакрытия
  - Вызывает `inventory_check_and_auto_close()`
- **Ответ**: Расхождения, статистика, `inventoryAutoClosed: true/false`

### 5. POST `/api/inventory/generate-report`
Генерирует CSV отчёт и сохраняет на сервер
- **Доступ**: admin, head, manager
- **Body**: `{ sessionId: "uuid" }` (опционально, берётся текущая/последняя сессия)
- **Действия**:
  - Получает полные данные через `/api/inventory/session-report`
  - Генерирует CSV с BOM для Excel
  - Загружает в Supabase Storage (`warehouse-files/inventory-reports/...`)
  - Создаёт audit log
- **Ответ**: 
```json
{
  "ok": true,
  "filePath": "inventory-reports/inventory-report-...-1737292800000.csv",
  "fileName": "inventory-report-...-1737292800000.csv",
  "publicUrl": "https://...",
  "sessionId": "uuid"
}
```

## 🔧 RPC Functions (PostgreSQL)

### `inventory_start()`
- Создаёт сессию
- Создаёт задания на все активные ячейки со статусом `pending`
- Возвращает количество созданных заданий

### `inventory_stop()`
- Закрывает сессию
- Возвращает количество незавершённых заданий

### `inventory_check_and_auto_close(p_session_id uuid)`
- Проверяет: все ли задания выполнены (`pending` count = 0)
- Если да → автоматически закрывает сессию
- Возвращает: `{ ok, autoClosed, pendingTasks, totalTasks }`

### `inventory_get_tasks(p_session_id uuid)`
- Возвращает список заданий с информацией о ячейках
- Используется для страницы прогресса

## 🎨 UI/UX

### Страница `/app/inventory`
- Показывает статус инвентаризации (активна / не активна)
- Кнопка "Начать инвентаризацию" (если не активна)
- Кнопка "Завершить инвентаризацию" (если активна, красная)
- Кнопка "📊 Посмотреть прогресс" (если активна, зелёная)
- Кнопка "📥 Скачать отчёт" (фиолетовая, всегда доступна если есть сессия)

### Страница `/app/inventory-progress`
- **Заголовок с прогресс-баром**:
  - Процент выполнения: 0-100%
  - Цвет: синий (в процессе), зелёный (100%)
  - Метрики: всего ячеек, отсканировано, осталось
  - Сообщение: "✅ Инвентаризация завершена!" при 100%

- **Секция "Ожидают сканирования"** (если есть):
  - Сетка ячеек (жёлтая рамка)
  - Код ячейки + тип

- **Секция "Отсканировано"** (если есть):
  - Список ячеек (построчно)
  - Цветная плашка (тип ячейки)
  - Код + тип
  - Кто и когда отсканировал
  - Галочка ✅

- **Автообновление**: каждые 5 секунд

### Режим "Инвентаризация" в ТСД (`/app/tsd`)
- Проверка статуса при открытии режима
- Блокировка если инвентаризация не активна
- Сканирование ячейки → сканирование заказов → сохранение
- Показ расхождений после сохранения

## 📄 Формат отчёта (CSV)

```csv
Отчёт по инвентаризации
Дата создания: 19.01.2026, 15:30:00
Сессия: a1b2c3d4-...
Статус: closed
Начата: 19.01.2026, 10:00:00
Завершена: 19.01.2026, 15:25:00

Общие показатели:
Всего ячеек,120
Отсканировано,120
Ячеек с расхождениями,5
Ожидалось заказов,450
Найдено заказов,448

Детали по ячейкам:
Ячейка,Тип,Ожидалось,Найдено,Не найдено,Лишние,Неизвестные
STOR-01,storage,15,15,0,0,0
STOR-02,storage,20,18,2,0,0
BIN-A1,bin,5,6,0,1,0
...
```

## 🔐 Безопасность и RLS

- **inventory_sessions**: пользователи видят только сессии своего склада
- **inventory_cell_counts**: доступ к заданиям только своего склада
- **inventory_cell_units**: доступ к данным только своего склада
- **Операции start/stop**: только admin, head, manager
- **Генерация отчётов**: только admin, head, manager
- **Сканирование**: все аутентифицированные пользователи склада

## 🚀 Workflow (полный цикл)

1. **Менеджер**: Начать инвентаризацию → создаются задания на все ячейки
2. **Система**: Все перемещения заблокированы
3. **Складчик 1**: ТСД → Инвентаризация → Сканирует STOR-01 → 15 заказов → Сохранить
4. **Система**: Задание STOR-01: `pending` → `scanned`
5. **Складчик 2**: Сканирует STOR-02 → 20 заказов → Сохранить
6. **Система**: Задание STOR-02: `pending` → `scanned`
7. **Менеджер**: Смотрит прогресс → 2/120 ячеек (1.7%)
8. ... (складчики сканируют все ячейки) ...
9. **Система**: Последняя ячейка отсканирована → автоматическое закрытие
10. **Система**: Перемещения разблокированы
11. **Менеджер**: Скачать отчёт → CSV с полной статистикой

## 📊 Метрики и аналитика

### Что отслеживается:
- Время начала/завершения инвентаризации
- Количество заданий (ячеек)
- Количество отсканированных ячеек
- Количество ячеек с расхождениями
- Недостачи (expected но не найдено)
- Излишки (найдено но не expected)
- Неизвестные заказы (не в базе)
- Кто сканировал какую ячейку и когда
- Длительность выполнения каждого задания
- Кто и когда сгенерировал отчёт

### Audit Events:
- `inventory.start` - запуск инвентаризации
- `inventory.stop` - ручное завершение
- `inventory.auto_close` - автоматическое завершение
- `inventory.cell_scan` - сканирование ячейки
- `inventory.generate_report` - генерация отчёта

## 📚 Документация

Полные инструкции добавлены в `/app/docs`:
- **Раздел "📋 Инвентаризация"**:
  - Инструкция для менеджеров (4 шага)
  - Инструкция для складчиков (4 шага)
  - Техническое описание процесса
  - Что происходит в системе на каждом этапе

## 🧪 Тестирование

### Сценарии для тестирования:

1. **Базовый flow**:
   - Запустить инвентаризацию
   - Отсканировать все ячейки
   - Проверить автозакрытие
   - Скачать отчёт

2. **Параллельная работа**:
   - Несколько складчиков сканируют разные ячейки одновременно
   - Проверить что нет конфликтов

3. **Расхождения**:
   - Отсканировать ячейку с недостачей
   - Отсканировать ячейку с излишками
   - Проверить отчёт

4. **Ручное завершение**:
   - Завершить инвентаризацию вручную с незавершёнными заданиями
   - Проверить что в отчёте указано количество незавершённых

5. **Блокировка перемещений**:
   - Попытаться переместить заказ во время инвентаризации
   - Должна быть ошибка HTTP 423 (Locked)

## 🎉 Итог

Система автоматически:
- ✅ Создаёт задания на все ячейки
- ✅ Отслеживает прогресс в реальном времени
- ✅ Закрывается когда всё выполнено
- ✅ Генерирует и сохраняет отчёты
- ✅ Синхронизирует базу с реальностью
- ✅ Блокирует перемещения во время работы
- ✅ Логирует все действия для аудита

Менеджер видит полную картину, складчики работают просто и эффективно!
