# Список заказов - Документация

## 📦 Обзор

Новая страница **"Список заказов"** (`/app/units`) предоставляет централизованный просмотр всех заказов на складе с фильтрацией и детальной информацией.

## 🔗 Доступ

**Кнопка:** В верхнем меню справа (TopBar): **"📦 Список заказов"** (между счётчиком задач и кнопкой SLA)

**Путь:** `/app/units`

**Права доступа:** Все авторизованные пользователи

## ✨ Функционал

### 1. Поиск по номеру заказа

**Поле поиска** с кнопками "Найти" и "✕" (очистить)

**Как работает:**
- Введите полный или частичный номер заказа (barcode)
- Нажмите "Найти" или Enter
- API выполняет поиск через `ILIKE` (регистронезависимый, частичное совпадение)
- Если есть активный поиск, показывается кнопка "✕" для очистки

**Примеры:**
- Поиск "123" найдет: `123456`, `4123`, `001230`
- Поиск "760" найдет: `4760167650635`

### 2. Таблица заказов

Отображает все заказы склада с колонками:

| Колонка | Описание | Источник |
|---------|----------|----------|
| **Штрихкод** | Уникальный номер заказа | `units.barcode` |
| **Товар** | Название товара | `units.product_name` |
| **Партнер** | Название мерча | `units.partner_name` |
| **Цена** | Стоимость товара | `units.price` |
| **Статус** | Текущий статус | `units.status` |
| **Ячейка** | Код и тип ячейки | `warehouse_cells` |
| **На складе** | Время нахождения | `now() - created_at` |
| **Создан** | Дата и время | `units.created_at` |

### 3. Фильтры по статусу

**Все** - показывает заказы во всех статусах

**Приёмка** (`receiving`) - заказы на приёмке

**Хранение** (`storage`) - заказы на хранении

**Пикинг** (`picking`) - заказы в процессе подготовки к отгрузке

**Отгрузка** (`shipping`) - заказы в отгрузке

**OUT** (`out`) - заказы отправленные через логистику

**Bin** (`bin`) - заказы в сортировке

### 4. Фильтры по времени

**Все** - показывает все заказы на складе

**> 24 часов** - заказы старше 24 часов (залежалые)

**> 48 часов** - заказы старше 48 часов (критичные)

**> 7 дней** - заказы старше недели (требуют немедленного внимания)

### 5. Цветовая индикация

**Возраст заказа:**
- 🟢 Зелёный - < 24 часов (норма)
- 🟡 Жёлтый - > 24 часов (внимание)
- 🟠 Оранжевый - > 48 часов (проблема)
- 🔴 Красный - > 7 дней (критично)

**Статусы:**
- 🔵 receiving - синий
- 🟣 bin - фиолетовый
- 🟢 storage - зелёный
- 🟠 picking - оранжевый
- 🔴 shipping - красный
- 🟣 out - фиолетовый

### 6. Навигация

**Клик по строке** → переход на детальную страницу заказа `/app/units/[unitId]`

**Кнопка "→"** → также переход на детальную страницу

## 🔌 API Endpoint

### `GET /api/units/list`

**Query Parameters:**
- `age` - фильтр по времени: `all | 24h | 48h | 7d`
- `status` - фильтр по статусу: `all | receiving | storage | picking | shipping | out | bin`
- `search` - поиск по штрихкоду (частичное совпадение, опционально)

**Response:**
```json
{
  "ok": true,
  "units": [
    {
      "id": "...",
      "barcode": "123456",
      "status": "storage",
      "product_name": "iPhone 15 Pro",
      "partner_name": "Apple Store",
      "price": 99999.99,
      "cell_code": "A1",
      "cell_type": "storage",
      "created_at": "2026-01-15T10:00:00Z",
      "age_hours": 72
    }
  ],
  "total": 150
}
```

**Логика фильтрации:**
```typescript
// Status filter
if (status && status !== 'all') {
  status = {status}  // Exact match
}
// Default: все статусы (receiving, storage, picking, shipping, out, bin, etc.)

// Age filter
switch (ageFilter) {
  case "24h": created_at < now() - 24 hours
  case "48h": created_at < now() - 48 hours
  case "7d":  created_at < now() - 7 days
  default:    все заказы
}

// Search filter
if (search) {
  barcode ILIKE '%{search}%'  // Case-insensitive partial match
}
```

## 🎨 UI/UX

### Адаптивная таблица
- Автоматическая сортировка по дате (новые сверху)
- Hover эффект на строках (светло-серый фон)
- Курсор pointer при наведении
- Smooth transitions

### Информативные бейджи
- Статусы с цветовой кодировкой
- Возраст с динамическим цветом
- Компактное отображение ячейки (код + тип)

### Удобная навигация
- Клик на любое место строки → детальная страница
- Кнопка "→" для явного перехода
- Кнопка "Назад" на детальной странице

## 📊 Use Cases

### 1. Поиск конкретного заказа
**Кто:** Все  
**Как:**
1. Открыть "📦 Список заказов"
2. Ввести номер заказа (полный или частичный)
3. Нажать "Найти"
4. Кликнуть на заказ → детали

### 2. Быстрый обзор склада
**Кто:** Manager, Head, Admin  
**Как:**
1. Нажать "📦 Список заказов" в TopBar
2. Увидеть все заказы в одной таблице
3. Оценить общее состояние (количество, возраст, статусы)

### 3. Поиск проблемных заказов
**Кто:** Manager, Ops  
**Как:**
1. Выбрать статус (например "OUT" для отправленных)
2. Выбрать фильтр "> 48 часов"
3. Увидеть все залежалые заказы в OUT
4. Кликнуть на заказ → посмотреть детали
5. Принять меры

### 4. Мониторинг партнёров
**Кто:** Head, Admin  
**Как:**
1. Просмотреть список заказов
2. Увидеть каких партнёров больше всего
3. Оценить ценовой диапазон

### 5. Переход к деталям
**Кто:** Все  
**Как:**
1. Найти нужный заказ в списке
2. Кликнуть → открыть детальную страницу
3. Посмотреть фото, историю, полную информацию

## 🔄 Интеграция с другими функциями

### Связь с Unit Detail Page
- Клик на заказ → `/app/units/[unitId]`
- Там можно:
  - Загружать фото
  - Редактировать информацию
  - Просматривать детальную историю

### Связь с TopBar
- Счётчик задач + Список заказов + SLA = полный контроль

### Связь с картой склада
- На карте можно найти ячейку с заказом
- В списке можно найти заказ и посмотреть где он находится

## 📈 Performance

### Оптимизации
- Использует `supabaseAdmin` для избежания RLS рекурсии
- JOIN с `warehouse_cells` для получения кода ячейки
- Расчёт возраста на уровне API (не в БД)
- Сортировка по индексированному полю `created_at`

### Лимиты
- **По умолчанию:** Нет лимита (показывает все)
- **Рекомендация:** Если заказов > 1000, добавить пагинацию

## 🎯 Будущие улучшения

1. **Пагинация** - для больших складов (> 1000 заказов)
2. **Расширенный поиск** - по названию товара, партнёру (сейчас только по штрихкоду)
3. **Сортировка** - по любой колонке
4. **Дополнительные фильтры:**
   - По партнёру
   - По ценовому диапазону
   - По ячейке
5. **Экспорт** - в Excel/CSV
6. **Массовые действия** - выбрать несколько заказов и выполнить действие

---

**✅ Готово к использованию!** Кнопка уже доступна в TopBar на всех страницах.
